const fieldIsRequired = 'This field cannot be empty';

const invalidEmail = 'Invalid email';

const shortPassword = 'Password must contain at least 6 characters';

const wrongEmail = 'Wrong email';

const wrongPassword = 'Wrong password';

const userAlreadyExists = 'User with this email or username already exists';

export {
  fieldIsRequired,
  invalidEmail,
  shortPassword,
  wrongEmail,
  wrongPassword,
  userAlreadyExists,
};
